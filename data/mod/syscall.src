let stdlib := clibpath + "syscall.so";

let syscall := cont {
    Write : func fd, buf, size {
        cmod(stdlib, "Syscall", "write", fd, buf, size);
    },

    Read : func fd, size {
        cmod(stdlib, "Syscall", "read", fd, size);
    },


};
