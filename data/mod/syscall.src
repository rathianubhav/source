let stdlib := clibpath + "syscall.so";

let syscall := cont {
    write : func fd, buf {
        cmod(stdlib, "Syscall", "write", fd, buf);
    },

    read : func fd, len {
        cmod(stdlib, "Syscall", "read", fd, len);
    }

};
